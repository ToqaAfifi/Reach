const VideoDetails = ({ channelId, channelTitle, description, liveBroadcastContent, publishTime, publishedAt, title }) => {

    return <div>
        {title}
    </div>
}

export default VideoDetails;